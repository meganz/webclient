{
  "name": "megawebapp",
  "version": "0.1.0",
  "main": "./main.js",
  "private": true,
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . megawebapp --all --out ~/megawebapp --ignore=node_modules/.* --ignore=start.sh --version 1.3.6 --overwrite --icon=./app/favicon.ico"
  },
  "devDependencies": {
    "electron-packager": "^8.0.0",
    "electron-prebuilt": "^1.3.6"
  },
  "dependencies": {
  }
}
